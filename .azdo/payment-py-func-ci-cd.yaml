name: payment-py-ci-cd

# trigger:
#   branches:
#     include:
#       - master
#   paths:
#     include:
#       - src/functions/payment-py/*

trigger: none
pr: none

variables:
  azureSubscription:
  functionAppName: "payment-func-py-app"
  pythonVersion: "3.11"
  adoEnvironment: "payment-py-ci-cd-env"

stages:
  - stage: Build
    displayName: "Build Python Function"
    jobs:
      - template: templates/t-build-func-py.yaml
        parameters:
          pythonVersion: $(pythonVersion)
          appPath: src/functions/payment-py
          requirementsPath: src/functions/payment-py/requirements.txt
          artifactName: payment-func-py

  - stage: Deploy
    displayName: "Deploy to Azure Function App"
    dependsOn: Build
    jobs:
      - deployment: Deploy
        environment: $(adoEnvironment)
        pool:
          vmImage: "ubuntu-latest"
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: functionapp
                - task: AzureCLI@2
                  inputs:
                    azureSubscription: "$(azureSubscription)"
                    scriptType: "bash"
                    scriptLocation: "inlineScript"
                    inlineScript: |
                      az functionapp deployment source config-zip \
                        --resource-group $(resourceGroup) \
                        --name $(functionAppName) \
                        --src $(Pipeline.Workspace)/functionapp/payment-func-py.zip
                  displayName: "Deploy Function App"
